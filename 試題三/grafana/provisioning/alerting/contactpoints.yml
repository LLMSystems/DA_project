apiVersion: 1

# 通知管道：把告警以 webhook 送到 notifier-sink 落地成通報紀錄。
contactPoints:
  - orgId: 1
    name: webhook-sink
    receivers:
      - uid: webhook-sink-receiver
        type: webhook
        settings:
          url: http://notifier-sink:9000/webhook
          httpMethod: POST
        disableResolveMessage: false
